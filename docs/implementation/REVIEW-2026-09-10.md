# PRD and pending-work review — 2026-09-10

Follow-up, 2026-09-13: shared policy constants and unrestricted review overrides
below have been replaced with candidate-private editable policy, content-based
policy versions and fact-bound evidence notes. Queue preparation/approval/dispatch
recheck eligibility. AI, embedding, scraping and enrichment failure cooldowns are
isolated. The table below remains the original review record; use
[PROFILE-ISOLATION-PLAN.md](PROFILE-ISOLATION-PLAN.md) and [TODO.md](TODO.md) for
current verification and remaining acceptance. This does not complete T01-T10.

Reviewed checkout: `6105d36` on `main`; working tree
was clean before this documentation review. Requirements remain accepted; the
full PRD is not implemented. [TODO.md](TODO.md) is the current execution summary,
[TASKS.md](TASKS.md) retains detailed acceptance items and historical evidence.

## Findings requiring implementation

| Priority | Finding and evidence | Required outcome |
| --- | --- | --- |
| High | `app/eligibility.py` uses shared pay thresholds, role exclusions and a candidate-specific policy label; `evaluate_job` reads sponsorship but ignores editable salary/location policy. | T02: move candidate rules into private, versioned settings; fresh profiles must inherit no other person's restrictions. Audit existing tracked candidate-specific literals without copying them into documentation. |
| High | `evaluate_job` returns a manual override before checking hard exclusions; the review API accepts a status without supporting facts. Queue insertion trusts that result. | T02/T08: evidence-backed review, invalidation after facts/policy change, and renewed hard-rule checks before preparation/submission. An override must not silently bypass an explicit incompatibility. |
| High | Eligibility treats role words anywhere in a description as exclusions, applies project-duration checks to full-time jobs, lacks currency-aware comparison and full remote/relocation restrictions. | T02: arrangement-specific duration, role-aware matching, currency/period/range rules, job-specific sponsorship and geographic fixtures. Current six evaluator tests do not establish full PRD acceptance. |
| High | `/queue/add` checks eligibility but does not enforce the PRD automatic-queue fit threshold, liveness, complete duplicate resolution or account identity; preparation does not repeat all gates. | T08: retain assisted review; implement and test complete preflight and confirmation audit before automatic submission. |
| High | Live read-only progress was idle with the last scoring counters at 0 of 600. | T04: diagnose saved unscored records and sanitized errors; prove a bounded successful scoring run. This snapshot does not establish the cause or exact remaining backlog. |
| Medium | `start-background.ps1` accepts only `DataRoot`; several documents still supplied removed `-MultiProfile`. | Corrected in this documentation change; profile mode is the production default. |
| Medium | PRD/plan introductions, TODO queue and T01 checkboxes contradicted integrated multi-profile code/tests. | Corrected current status; historical evidence is explicitly archival. T01 remains partial against its broader acceptance requirements. |
| Medium | README mixed 14 and 11 sources and made universal autofill/privacy claims. | Corrected to the 11 registered adapters and qualified external-service/autofill behavior. Source availability still requires live verification. |

## PRD coverage and complete pending inventory

Unchecked TASKS items remain acceptance work, including partially implemented
features. Existing modules alone do not prove the stricter new requirements.

| Milestone / PRD area | Implemented evidence | Pending work |
| --- | --- | --- |
| T00 foundation | External storage, preserved migration evidence, CI and recorded baselines | No new setup blocker; rerun release checks when implementation changes. |
| T01 candidate isolation | `candidates.py`, `candidate_runtime.py`, `multi_profile.py`, profile-scope UI, extension pairing; concurrent-profile and lifecycle tests | Windows keyring; secret-free portable export/import; active profile/policy version updates; broader older-schema and external-reference recovery coverage. Employer-account verification remains manual. |
| T02 onboarding / eligibility | Resume text extraction and editable profile; evaluator, queue rejection, feed/detail review, compensation metadata, resume activation/rescore confirmation | Fix high-priority findings above; reviewed evidence-linked facts and original-file retention; complete editable arrangements/pay/location/authorization/availability; versioned selective invalidation; sourced historical sponsorship with timestamps; complete hard-rule fixtures. |
| T03 discovery / history | 11 registered scrapers, source due times and progress, exact hash/similarity helpers, shared rate limiter | Complete source method/credential/coverage audit; JobSpy; live Lever/Ashby and broader Greenhouse verification; raw snapshots/requisition IDs/canonical aliases; initial 30-day and overlapping incremental windows; uncertain duplicate review; authoritative liveness; complete per-source health/next-run UI; coordinated request budgets. |
| T04 matching / materials | Grounded scoring correction retry, thinking/truncation handling, PDF/DOCX and cover-letter modules; recorded synthetic checks | Live scoring recovery; GPU/context/concurrency evidence and 30-case quality benchmark; full PRD fit-band validation; versioned caches; distinct reviewer with at most two revisions; immutable document versions/hashes; factual real-shortlist and native Word layout checks. |
| T05 assisted workflow / dashboard | Candidate UI, pipeline, links/downloads and pairing implemented; synthetic profile smoke recorded | Real discover-review-prepare-assisted-apply-track demonstration; real-site autofill validation; manual vs independently confirmed application evidence; persisted run/task progress; daily target and shortfall view; complete one-click assisted-release acceptance. |
| T06 durable operation / costs | Per-candidate in-process schedules, owned tasks, source limits, installation lock and live progress | Durable runs/tasks/events and idempotency; transactional leases/heartbeat/cancel/retries; recovery and uncertain-submit reconciliation; configurable discovery/Gmail/dispatch cadence; fair model/browser scheduling; installation cost reservations/cap and allocations; persisted SSE reconnect; Windows sign-in/crash restart, offline/sleep catch-up. |
| T07 Gmail / application history | Existing manually maintained pipeline is available | Read-only OAuth and protected tokens; configurable 12-month import; portal-only/LinkedIn history and coverage gaps; incremental cursors, expiry recovery and deduplication; evidence-linked status reconciliation; conflicts, chronology and manual-correction preservation. |
| T08 automatic applications / audit | Assisted extension and preparation/review queue exist | Adapter capability contract; Greenhouse then Lever submit adapters; all preflight gates; exact units/options and known required answers; conditional fields/uploads; immutable question/answer/document/destination snapshot; independent confirmation and uncertain state; failure fixtures; one explicitly authorized real application per adapter before unattended enablement. |
| T09 broader product | Existing research/contact/follow-up/Q&A/interview/notification/analytics tools | Live additional-board/Dice and wider ATS validation; sourced/timestamped research; reviewed recruiter/outreach/follow-up drafts; Q&A/interview refinements; candidate-scoped configurable notification channels; Notion one-way sync; observed conversion metrics with denominators; sourced relocation cost data. Existing predictions are not verified hiring probabilities. |
| T10 packaging / release | Start/stop scripts, backup/migration CLI, runbook and smoke script | Setup/update/backup/restore automation and shortcut; fresh-machine/separate-install and upgrade/recovery drills; complete account/model/budget documentation as features land; privacy/secret/license audit; final regression and acceptance evidence; reviewed fork PR and tested release. |

## Mandatory remaining regression acceptance

Keep all 13 cross-cutting cases in TASKS.md visible: per-candidate applications to
the same requisition; distinct requisitions; employer identity and switch races;
pay missing/overlap/exclusion; sponsorship refusal/unknown; C2C versus sponsorship;
remote residence versus headquarters/relocation; email chronology; power loss
after submission; prompt injection across listings/resumes/email; concurrent
budget reservations; factual claim provenance; scoped reset/delete/export.
Existing isolation and scoring tests cover parts, not this entire acceptance set.

## Review evidence and limits

- Inspected current PRD, plan, TODO, TASKS, handoff, storage guide, README/runbook,
  source registry, eligibility/queue/review/settings code, runtime/scheduler,
  launch scripts, dependencies, CI and relevant test definitions.
- Searched application, extension, tests, scripts and documentation for TODO,
  FIXME and unchecked tasks. Active unchecked backlog is in TASKS.md; old
  `docs/plans` design recipes are historical context, not proof of completion or
  a second current execution queue.
- Live GET health returned healthy, DB OK, profile mode; GET runtime progress
  showed no active work. No personal identifiers or raw runtime data are included.
- Prior recorded results: backend **724**, frontend **187**, extension **472**
  passed. These are historical evidence, not new runs from this review.
- This change is documentation-only. Verify local links/anchors, script parameters,
  API examples and whitespace; no application regression run, restart, migration,
  paid AI call, message, real application or fresh-install drill was performed.
- Completed review checks: 43 local links/anchors passed; runbook health/progress
  routes matched implementation; both PowerShell launchers parsed successfully;
  corrected startup examples match the current `DataRoot` parameter;
  `git diff --check` passed. External links were not fetched in this local audit.

Next implementation order: T02 generic policy and hard-gate repair, bounded T04
scoring recovery, remaining T01 portability/security and T03/T04 quality work,
then demonstrate T05. Retain T06–T10 with their existing dependencies.
